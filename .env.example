# =============================================================================
# 🏠 RENTAL PROPERTY CHATBOT CONFIGURATION
# =============================================================================
# Professional-grade configuration for production deployment
# Review and adjust these settings based on your requirements

# -----------------------------------------------------------------------------
# 🤖 AI MODEL CONFIGURATION
# -----------------------------------------------------------------------------
# OpenAI API key - Required for AI functionality
OPENAI_API_KEY=your-openai-api-key-here

# Model selection - Choose based on requirements:
# - gpt-3.5-turbo: Fast, cost-effective, good for most queries ($0.0015/$0.002 per 1K tokens)
# - gpt-4: Higher accuracy, more expensive ($0.03/$0.06 per 1K tokens)
# - gpt-4-turbo: Latest model, balanced performance/cost
OPENAI_MODEL=gpt-3.5-turbo

# Temperature (0.0-2.0): Lower = more consistent, Higher = more creative
# Recommended: 0.1-0.3 for property queries
TEMPERATURE=0.2

# Max tokens per response: Lower = faster/cheaper, Higher = more detailed
# Recommended: 300-500 for property descriptions
MAX_TOKENS=400

# -----------------------------------------------------------------------------
# 📊 DATA CONFIGURATION
# -----------------------------------------------------------------------------
# Data source selection:
# - json: Local JSON file (recommended for production)
# - api: Live API endpoint (requires network access)
DATA_SOURCE=json

# Maximum properties to load and analyze
# Higher = more comprehensive results, Lower = faster responses
MAX_PROPERTIES=18

# JSON file path (relative to project root)
JSON_DATA_PATH=./data/properties.json

# -----------------------------------------------------------------------------
# 🔧 PERFORMANCE & OPTIMIZATION
# -----------------------------------------------------------------------------
# Response timeout in milliseconds
RESPONSE_TIMEOUT=30000

# Enable detailed logging for debugging
DEBUG_MODE=false

# Enable cost tracking and analytics
ENABLE_COST_TRACKING=true

# Cache system prompt for better performance
CACHE_SYSTEM_PROMPT=true

# -----------------------------------------------------------------------------
# 🎨 USER EXPERIENCE
# -----------------------------------------------------------------------------
# Enable animated thinking indicators
ENABLE_ANIMATIONS=true

# Animation style: dots, brain, gears, pulse, search
ANIMATION_STYLE=brain

# Welcome message customization
WELCOME_MESSAGE=custom

# Show performance metrics to users
SHOW_PERFORMANCE_METRICS=true